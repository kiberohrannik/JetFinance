package com.kiber.jet.finance.core.service

import com.kiber.jet.finance.core.domain.CardData

class HolderListCardVerifier extends CardVerifier {

  override def verify(cardData: CardData): Boolean = {
    ???
  }

}
